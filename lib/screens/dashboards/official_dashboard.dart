import 'package:flutter/material.dart';

class OfficialDashboard extends StatelessWidget {
  const OfficialDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Government Official Dashboard')),
      body: const Center(child: Text('🏛️ Official Dashboard Placeholder')),
    );
  }
}
